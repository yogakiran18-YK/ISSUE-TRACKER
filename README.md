Enhanced Issue Tracker — Fun Colorful UI

Run locally:

1. Start MongoDB (local) or set MONGO_URI in server/.env
2. Install and run server:
   cd server
   npm install
   npm run seed
   npm run dev
3. Install and run client:
   cd ../client
   npm install
   npm run dev

Open http://localhost:5173

Note: The project includes a colorful UI and supports assignee & priority fields.
